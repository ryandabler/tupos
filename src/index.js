"use strict";

const types = require("./constants");
const {
    typeOf,
    isIterable,
    isPrimitive
} = require("./tupos");

module.exports = {
    types,
    typeOf,
    isIterable,
    isPrimitive
}