"use strict";

const FUNCTION = "Function";
const OBJECT = "Object";
const NUMBER = "Number";
const ARRAY = "Array";
const STRING = "String";

module.exports = {
    FUNCTION,
    OBJECT,
    NUMBER,
    ARRAY,
    STRING
}